# Release 0.6.2

Adds `jep_api_token` for authenticated API signing. Pass `${{ secrets.JEP_API_TOKEN }}`; the action masks the value in workflow logs. The committed Node 24 bundle is rebuilt and checked in CI.
